"""pommes_eur.solve — build/solve/export + adequacy diagnostics.

- ``runner``    : sanitize inputs, enforce WACC/CAPEX, build_model + solve, write NetCDF,
                  export prices/capacities (generic POMMES solve machinery).
- ``adequacy``  : ex-ante / ex-post adequacy diagnostics (LOLE/ENS).
"""
